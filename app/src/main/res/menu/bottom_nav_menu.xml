<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <item
        android:id="@+id/navigation_push"
        android:title="Push"
        android:icon="@drawable/push" /> <!-- Убедитесь, что у вас есть иконка -->
    <item
        android:id="@+id/navigation_sms"
        android:title="Sms"
        android:icon="@drawable/sms" /> <!-- Убедитесь, что у вас есть иконка -->
    <item
        android:id="@+id/navigation_dlq"
        android:title="DLQ"
        android:icon="@drawable/dlq" /> <!-- Убедитесь, что у вас есть иконка -->
    <item
        android:id="@+id/navigation_journal"
        android:title="Journal"
        android:icon="@drawable/journal" />
    <item
        android:id="@+id/navigation_options"
        android:title="Options"
        android:icon="@drawable/options" /> <!-- Убедитесь, что у вас есть иконка -->
</menu>
